
// export const load = async () => ({ url }) => {
// 	const page = Number(url.searchParams.get('page') ?? 1);
// 	const limit = Number(url.searchParams.get('limit') ?? 10);
//
// 	// const round: Round = await
// 	// // const pairingsResponse = await loadPairings(roundId, page, limit);
// 	// // const pairings: Pairing[] = await Promise.all(pairingsResponse.map( async (pairing) => {
// 	// //
// 	// // }));
// }